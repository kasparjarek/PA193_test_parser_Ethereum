language: cpp

compiler:
  - clang
  - gcc

before_install:
  - sudo apt-get -qq update
  - sudo apt-get install -y libboost-all-dev
  - sudo add-apt-repository universe
  - sudo apt-cache policy libcrypto++-dev
  - sudo apt-get install -y libcrypto++-dev=5.6.4-8

before_script:
  - mkdir build
  - cd build
  - cmake ..
  - make

script:
  - ./test_parser