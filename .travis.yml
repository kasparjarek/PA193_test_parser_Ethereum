language: cpp

compiler:
  - clang
  - gcc

before_install:
  - sudo add-apt-repository universe
#  - sudo apt-get -qq update
  - sudo apt-get update
  - sudo apt-get install -y libboost-all-dev
  - sudo apt-cache policy libcrypto++6
  - sudo apt-get install -y libcrypto++6

before_script:
  - mkdir build
  - cd build
  - cmake ..
  - make

script:
  - ./test_parser