language: cpp

compiler:
  - clang
  - gcc

before_install:
  - sudo apt-get -qq update
  - sudo apt-get install -y libboost-all-dev

before_script:
  - mkdir build
  - cd build
  - cmake ..
  - make

script:
  - ./test_parser